#!/usr/bin/env bash

HOST="$(hostname)"

case "$HOST" in
    hexagon)
        exec sudo nixos-rebuild "$1" --flake ~/config
        ;;
    hexamac)
        exec darwin-rebuild "$1" --flake ~/config
        ;;
esac
