#!/usr/bin/env bash

HOST="$(hostname -s)"

case "$HOST" in
    hexagon)
        exec sudo nixos-rebuild "$1" --flake "$(realpath ~/config)"
        ;;
    hexamac)
        exec darwin-rebuild "$1" --flake "$(realpath ~/config)"
        ;;
esac
